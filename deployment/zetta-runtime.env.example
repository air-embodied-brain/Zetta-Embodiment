# Optional host-local model services.
ZETTA_VLA_ENDPOINT=http://127.0.0.1:18091
# Optional: configure CONTACT_GRASPNET_URL only when using an independently
# deployed service that implements the documented /health and /propose API.
GRASPGEN_URL=http://127.0.0.1:18093
