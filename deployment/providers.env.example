# Copy this file outside the repository, chmod it to 0600, and replace every
# placeholder before starting the provider broker. Never commit the populated file.
export PRIMARY_PROVIDER_API_KEY='<secret>'
export ZETTA_API_PROVIDER_BROKER_API_KEY='<random-local-broker-secret>'
export ZETTA_API_PROVIDERS='{
  "cooldown_seconds": 30,
  "providers": [{
    "name": "primary",
    "provider": "openai-responses",
    "base_url": "https://provider.example/v1",
    "api_key_env": "PRIMARY_PROVIDER_API_KEY",
    "model": "openai-responses:your-model",
    "price": 1.0,
    "max_concurrency": 4
  }]
}'
export ZETTA_API_PROVIDER_BROKER_URL='http://127.0.0.1:4110'
export ZETTA_PROVIDER_INITIAL_CONCURRENCY=4
export ZETTA_PROVIDER_MAX_CONCURRENCY=4
